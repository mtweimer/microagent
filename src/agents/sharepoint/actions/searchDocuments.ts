import type { AgentExecutionResult } from "../../../core/contracts.js";

export async function searchDocumentsAction(): Promise<AgentExecutionResult> {
  return {
    status: "error",
    message: "SharePoint agent is scaffolded but not implemented yet."
  };
}
